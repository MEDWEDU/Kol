# Base URL for API requests from the client (optional if using the Vite proxy)
VITE_API_URL=http://localhost:5000

# Client URL (useful for displaying links / redirects)
VITE_CLIENT_URL=http://localhost:5173
